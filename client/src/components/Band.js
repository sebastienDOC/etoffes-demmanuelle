import React from "react";

const Band = () => {
	return (
		<div className="bg-black text-white flex justify-center items-center">
			<h2 className="font-titleFont mx-5 h-20 sm:h-10 flex justify-center items-center text-center">
				Première commande ? -15% avec le code WELCOME15 (uniquement sur le site)
			</h2>
		</div>
	);
};

export default Band;
